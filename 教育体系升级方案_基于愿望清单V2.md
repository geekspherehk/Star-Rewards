# Star-Rewards 教育体系升级方案
### 基于《愿望清单体系 V2 · 全人版》的产品经理分析

> 分析对象：`stellar.gaocaihk.com`（Star-Rewards 儿童行为激励网站）
> 对标文档：`Star_Rewards_愿望清单体系.pdf`（产品规范与模板库 V2 · 全人版）
> 目标：把当前"积分 → 换物"的单一激励模型，升级为"全人成长"的教育体系
> 日期：2026-08-13

---

## 0. 执行摘要（Executive Summary）

| 维度 | 现状 | 目标（V2 全人版） |
|---|---|---|
| 核心模型 | 行为加分 → 礼物兑换（ extrinsic-only） | 8 大素养方向 → 孩子自设愿望 → 努力定价 → 徽章 + 成长报告 |
| 激励观 | 积分换实物，"给越多越乖" | **去贿赂化**：按努力量强化，分享线下兑现，自己花费设上限 |
| 类别体系 | `behaviors.dimension` 字段已存在但未使用 | 激活为 8 大成长方向（全人玫瑰图） |
| 习惯养成 | 仅有"连续天数"streak，无内化机制 | 打卡"退出协议"：建立→稳定→内化，撤卡是荣耀 |
| 家长负担 | 家长手动记行为、定分值 | 模板库 + 主类别原则 + 成长指标自动生成报告 |
| 内在动机 | 易被"德西效应"削弱 | 两段式自主（每月主打 1 瓣）+ 软提示，无硬性门槛 |

**一句话结论**：当前系统已具备升级的地基（`behaviors.dimension`、`milestones`/`growth_notes`/`child_voice` 成长层、`computeBestStreak`、i18n 中英双语），但把"愿望=礼物=换物"三者混为一谈。升级的本质是**把"愿望"从"兑换目录"重定义为"挂在 8 大素养下的成长目标"**，并补上打卡、徽章、成长指标三层机制。

---

## 1. 方法论解码（Methodology Decoding）

### 1.1 五大设计原则
1. **类别即能力** —— 每类愿望本身在培养一种素养，兑换的同时就在练能力，不额外增加家长负担。
2. **自主 + 结构引导** —— 孩子每月自选 1 个"本月主打瓣"；系统只做软提示与"全能小星星"奖励，不设硬门槛（SDT 自主性）。
3. **去贿赂化** —— 自己花费设预算上限练"取舍"；分享类由家长线下兑现；星级按"努力量"定价，让坚持而非索取被强化。
4. **可测量** —— 每类配"萌芽 → 成长 → 绽放"三级行为指标，进步看得见、可对照、可迭代。
5. **机制防退化** —— 打卡有"退出协议"、分类有"主类别原则"，防止打卡依赖与"挑容易的"套利。

### 1.2 八类体系（全人教育玫瑰图）
| 类别（新名 · 角色徽章） | 培养目标 | 理论依据 |
|---|---|---|
| 🔓 新本领解锁 · 🎓小学霸 | 自驱力、成长型思维 | SDT-胜任感、Dweck |
| 💰 星币宝藏屋 · 💰小掌柜 | 理财能力、延迟满足 | 儿童理财"四罐法" |
| ❤️ 爱心发射站 · ❤️暖心侠 | 分享能力、共情 | SEL 社会意识 |
| ☀️ 开心特权日 · ☀️开心宝 | 关系联结、幸福感 | PERMA-关系 |
| 🗓️ 时间大本营 · 🗓️规划师 | 规划能力、执行功能 | 执行功能、自我管理 |
| ⚔️ 勇者试炼塔 · ⚔️挑战王 | 抗挫力、自我效能 | Bandura 自我效能 |
| 🌱 身体加油站 · 🏃活力星 | 健康习惯、身体素养 | 健康行为理论 HBM |
| 🎨 艺术创造家 · 🎨小艺术家 | 审美、创造力、表达 | Gardner 多元智能 |

> 八片花瓣 = 八种素养：**自驱 · 理财 · 共情 · 关系 · 规划 · 抗挫 · 健康 · 审美**。

### 1.3 三层模型（别混在一起）
| 层 | 是什么 | 是否强制 |
|---|---|---|
| **8 大类别** | 成长方向（目标分类） | 自主+引导：每月主打 1 瓣，覆盖越多解锁越多徽章 |
| **愿望** | 挂在某类别下的具体目标 | 孩子自选（按主类别原则归属） |
| **每日打卡** | 独立的习惯养成工具（cue），可挂在任何愿望下 | 可选，且有退出协议（不终身绑定） |

### 1.4 关键内置机制（产品规则）
- **两段式自主**：每月自选 1 个"本月主打瓣"；系统软提示未覆盖类别（"去⚔️试一次？"）；覆盖满 8 类解锁"全能小星星"徽章。无硬性门槛。
- **主类别原则**：每个愿望选 1 个主类别（主要培养意图）+ 至多 2 个关联类别；覆盖统计只按主类别计，杜绝"一箭多雕"。
- **努力系数定价**：星数 = 坚持天数 × 难度系数（体验型 1–2 星 / 坚持型 3–5 星 / 挑战型 5–10 星，家长可调），让"难而坚持"获得更强强化。
- **打卡退出协议**：建立（7 天）→ 稳定（21 天）→ 内化（撤卡）；达标弹窗"恭喜，这个习惯已经是你的了，把打卡位让给下一个目标？"——撤卡是荣耀，不是失败。
- **分享类线下兑现**：❤️ 爱心发射站由家长在 App 记录、线下完成（不自动发钱物），保持真实与温度。
- **自己花费设上限**：💰 星币宝藏屋需设预算星数上限，培养"取舍"而非"索取"。
- **角色徽章**：每类攒到阈值即解锁对应徽章；集齐 8 枚解锁"全能小星星"。
- **成长指标（萌芽·成长·绽放）**：家长每周对照勾选一次，勾选记录自动生成"成长报告"，话术遵循成长型思维（夸努力/策略/过程）。
- **愿望模板库**：分龄内置（低龄 6–8 / 小学高 9–12 / 中学 13–18 / 成人 19+）。

---

## 2. 现状对照与差距分析（Gap Analysis）

### 2.1 当前系统资产（已具备）
- `behaviors.dimension` 字段**已存在**（varchar 20）但前端未使用 → 天然承接 8 大类别。
- `milestones` / `growth_notes` / `child_voice` 已是"成长纵向层"，与 V2 的"成长报告"方向一致。
- `computeBestStreak()` 已实现连续天数计算 → 可改造为打卡"退出协议"的底层。
- `redeemed_gifts` + `showCelebrationCertificate()` 已有庆祝闭环 → 可升级为徽章体系。
- `i18n.js`（zh/en）已就绪 → 8 类/模板库可直接双语化（符合"中英双语"差异化定位）。
- 73 个行为模板 + 三模式 → 可直接映射为"愿望模板库"的骨架。

### 2.2 差距矩阵
| V2 要素 | 现状 | 缺口 | 升级动作 |
|---|---|---|---|
| 8 大类别 | `dimension` 空置 | 无枚举、无 UI、无玫瑰图 | 激活 dimension 为 8 码 + 全人玫瑰仪表盘 |
| 愿望层 | 仅有 `gifts`（=兑换物） | "愿望=目标"与"礼物=奖励"混淆 | 新增 `wishes` 表，与 `gifts` 解耦 |
| 努力定价 | 行为固定 `points` | 无难度系数 / 坚持天数 | 新增 `effort_type` + `difficulty_coef` |
| 打卡 + 退出协议 | 仅 streak | 无建立/稳定/内化三阶段 | 新增 `checkins` 表 + 阶段状态机 |
| 本月主打瓣 | 无 | — | 新增 `monthly_focus` |
| 角色徽章 | 仅庆祝证书 | 无 8 徽章 + 全能星 | 新增徽章解锁逻辑 + 徽章墙 |
| 成长指标 | 无 | — | 新增 `growth_indicators` 周勾选 + 报告 |
| 主类别原则 | 无 | 一愿望可多类套利 | `wishes.related_categories`（≤2） |
| 分享线下兑现 | gifts 全线上兑换 | ❤️类被当普通礼物兑换 | gifts 加 `fulfillment=online/offline` |
| 预算上限 | 无 | 💰类无取舍训练 | profile 级 `budget_cap` |

---

## 3. 升级蓝图（Upgrade Blueprint）

### 3.1 信息架构重构（三层为一等公民）
```
App Shell
├─ 全人玫瑰仪表盘（8 瓣覆盖度 + 本月主打瓣 + 全能星进度）
├─ 愿望清单（按 8 类分组；每愿望含 努力定价 / 打卡入口 / 成长指标）
├─ 礼物兑换（= 💰 星币宝藏屋 + 其他可兑换体验；预算上限；❤️线下兑现）
├─ 徽章墙（8 角色徽章 + 全能小星星）
└─ 成长纪念册（既有 milestones/notes/voice + 新增 growth report）
```

### 3.2 数据模型升级（MySQL / PDO 兼容）
```sql
-- behaviors：激活 dimension，承接 8 类 + 关联类 + 努力定价 + 关联愿望
ALTER TABLE behaviors
  MODIFY dimension ENUM('self_drive','finance','empathy','relationship',
                        'planning','resilience','health','art') NULL,
  ADD related_categories JSON NULL,        -- 至多 2 个关联类别
  ADD effort_type ENUM('experience','persistence','challenge') NULL,
  ADD difficulty_coef TINYINT NULL DEFAULT 1,  -- 家长可调 1..10
  ADD wish_id BIGINT NULL,                   -- 关联愿望（可选）
  ADD KEY idx_behavior_wish (wish_id);

-- 新增：愿望（孩子自设的成长目标，挂在 8 类下）
CREATE TABLE wishes (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  family_id BIGINT NOT NULL, profile_id BIGINT NOT NULL, user_id BIGINT NOT NULL,
  category ENUM('self_drive','finance','empathy','relationship',
                'planning','resilience','health','art') NOT NULL,
  related_categories JSON NULL,            -- 主类别原则：≤2
  title VARCHAR(255) NOT NULL,
  effort_type ENUM('experience','persistence','challenge') NOT NULL DEFAULT 'persistence',
  target_days INT NULL,                     -- 坚持型/挑战型用
  difficulty_coef TINYINT NOT NULL DEFAULT 3,
  star_value INT NULL,                      -- = target_days * difficulty_coef（自动算）
  status ENUM('active','done','archived') NOT NULL DEFAULT 'active',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  KEY idx_wish_profile (profile_id)
);

-- 新增：每日打卡（习惯工具 + 退出协议）
CREATE TABLE checkins (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  wish_id BIGINT NOT NULL, profile_id BIGINT NOT NULL,
  check_date DATE NOT NULL,
  stage ENUM('build','stable','internalized') NOT NULL DEFAULT 'build',
  PRIMARY KEY (wish_id, check_date),        -- 每天每愿望一次
  KEY idx_checkin_profile (profile_id)
);

-- 新增：本月主打瓣
CREATE TABLE monthly_focus (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  profile_id BIGINT NOT NULL,
  ym CHAR(7) NOT NULL,                       -- '2026-08'
  focus_category ENUM('self_drive','finance','empathy','relationship',
                      'planning','resilience','health','art') NOT NULL,
  UNIQUE KEY uniq_profile_month (profile_id, ym)
);

-- 新增：成长指标周勾选（萌芽/成长/绽放）
CREATE TABLE growth_indicators (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  profile_id BIGINT NOT NULL, family_id BIGINT NOT NULL,
  category ENUM('self_drive','finance','empathy','relationship',
                'planning','resilience','health','art') NOT NULL,
  level ENUM('sprout','grow','bloom') NOT NULL,
  week_start DATE NOT NULL,
  noted_by BIGINT NOT NULL,                  -- 家长 user_id
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  KEY idx_gi_profile (profile_id)
);

-- 徽章：定义表可硬编码在 themes/i18n；解锁记录用下表
CREATE TABLE user_badges (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  profile_id BIGINT NOT NULL,
  badge_code VARCHAR(20) NOT NULL,           -- self_drive..art | all_star
  unlocked_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uniq_profile_badge (profile_id, badge_code)
);

-- gifts：加履行方式与类别，承接 💰 星币宝藏屋 + 可兑换体验
ALTER TABLE gifts ADD fulfillment ENUM('online','offline') NOT NULL DEFAULT 'online';
ALTER TABLE gifts ADD category ENUM('finance','relationship','health','art') NULL; -- 主要兑换类
ALTER TABLE profiles ADD budget_cap INT NULL;   -- 💰 类预算星数上限
```

### 3.3 核心机制实现要点
| 机制 | 规则 → 实现 | 防什么 |
|---|---|---|
| 努力定价 | `star_value = target_days × difficulty_coef`；体验型给固定 1–2，挑战型 5–10 | 防止"挑容易的"套利 |
| 主类别原则 | `wishes.related_categories` ≤2；覆盖度统计只计主类 | 一箭多雕 |
| 两段式自主 | 每月 `monthly_focus`；未覆盖类软提示（"🌱 这个月还没浇水哦"） | 硬门槛伤内在动机 |
| 打卡退出协议 | `checkins.stage`：满 7 天→stable，满 21 天→internalized；内化弹窗释放打卡位 | 为打卡而打卡 |
| 分享线下兑现 | `gifts.fulfillment='offline'`（❤️类）：App 记录，不自动发星/物 | 稀释真实温度 |
| 预算上限 | `profiles.budget_cap`；💰类兑换前校验 | 索取而非取舍 |
| 角色徽章 | 每类累计星达阈值→`user_badges`；集齐 8→`all_star` | — |
| 成长报告 | `growth_indicators` 周勾选→自动汇总萌芽/成长/绽放 | 进步不可见 |

### 3.4 前端 / UX 升级
- **全人玫瑰图 Dashboard**：8 瓣雷达/玫瑰可视化（可用 Canvas 或 SVG，复用现有 `poster` 渲染经验），中心点显示"全能小星星"进度。
- **愿望清单重构**：按 8 类折叠分组；每愿望卡片显示 努力定价徽标、打卡按钮、三级成长指标勾选。
- **打卡组件**：轻量"点一下"cue；达阶段弹窗（含"撤卡是荣耀"文案）。
- **徽章墙**：8 角色徽章 + 全能星，未解锁置灰。
- **成长报告**：每周自动生成（萌芽→成长→绽放 对照），话术模板走成长型思维。
- **家长说明卡**：每类一句（"🔓 练自驱力，多夸努力"），可截图分享（契合裂变策略）。

---

## 4. 分阶段路线图（Phased Roadmap）

> 遵循项目"部署铁律②"：每阶段本地自检 → 提交 git → 展示变更摘要 → **用户批准** → 部署。

**Phase 0 · 地基（1 周）**
- 激活 `behaviors.dimension` 为 8 枚举；迁移历史数据（空→NULL，不影响）。
- 新增 `wishes` / `checkins` / `monthly_focus` / `growth_indicators` / `user_badges` 表。
- 风险：Hostinger PDO 列对齐、lastInsertId 顺序（沿用既有坑经验）。

**Phase 1 · 核心循环（2 周）—— 愿望 + 努力定价 + 徽章**
- 愿望 CRUD（挂在 8 类，主类别原则，努力定价自动算星）。
- 玫瑰图 Dashboard + 徽章墙 + 全能星。
- 改造 `addBehavior` / `redeemGift` 以写入 `dimension`/`wish_id`。
- 复用 `showCelebrationCertificate` 升级为徽章庆祝。

**Phase 2 · 习惯内化（2 周）—— 打卡 + 退出协议 + 本月主打瓣**
- 打卡组件 + `checkins` 状态机（build/stable/internalized）。
- 每月主打瓣软提示；21 天"游戏化挑战目标"文案（强调"断了就续"）。
- `computeBestStreak` 改造为阶段驱动。

**Phase 3 · 可测量（1.5 周）—— 成长指标 + 成长报告 + 家长卡**
- 周勾选 UI + `growth_indicators`；自动生成成长报告（成长型思维话术）。
- 家长说明卡（可截图）。

**Phase 4 · 规模化（2 周）—— 模板库 + 分龄 + 双语**
- 内置 8 类 × 4 龄段愿望模板（直接复用现有 73 模板结构）。
- i18n 全量双语（zh/en），契合"中英双语"差异化。
- ❤️ 线下兑现 + 💰 预算上限上线。

---

## 5. 风险与权衡（Risks & Trade-offs）

1. **复杂度 vs. "不额外增加家长负担"**：V2 原则①要求零额外负担。应对：模板库 + 自动报告 + 软提示，把配置成本压到最低；首屏仍保持极简。
2. **内在动机保护**：过度引导会触发德西效应。应对：坚持"两段式自主、无硬门槛、撤卡是荣耀"。
3. **数据迁移**：现有 `gifts` 全是线上兑换。应对：Phase 4 才引入 offline/budget，存量礼物默认 `online`，不破坏旧数据。
4. **i18n 一致性**：新增 8 类/模板需同步 zh/en 文案，沿用 `i18n.js` 缺失回退默认文本规则。
5. **CDN 缓存**：改 JS/CSS 后必 bump `?v=` 与 `sw.js` CACHE_NAME（既有纪律）。
6. **范围蔓延**：八类全做易过载。应对：Phase 1 先跑通"愿望+定价+徽章"最小闭环，再扩打卡与报告。

---

## 6. 成功指标（Success Metrics）

| 指标 | 定义 | 目标 |
|---|---|---|
| 八类覆盖率 | 活跃孩子覆盖的类别数 / 8 | 月活均 ≥ 4 类 |
| 本月主打瓣参与率 | 设了 monthly_focus 的 profile 占比 | ≥ 60% |
| 打卡内化率 | 达 internalized 的打卡数 / 总打卡数 | ≥ 30%（验证退出协议有效） |
| 全能星解锁数 | 集齐 8 徽章的孩子数 | 增长 trend |
| 成长报告生成率 | 周勾选 ≥1 次的家族占比 | ≥ 40% |
| 分享裂变 | 家长说明卡截图分享次数（埋点） | 监控 WAF/D1·D7 |

---

## 7. 给工程/设计的下一步（Next Steps）

1. **后端**：在 `api/index.php` 增加 `wishes` / `checkins` / `monthly_focus` / `growth_indicators` / `user_badges` 路由（沿用 PDO 预处理 + JWT）。
2. **前端**：`script.js` 新增愿望/打卡/玫瑰图模块；`themes.js` 增加 8 类配色与徽章 SVG。
3. **埋点**：在 `analytics_events` 增加 `set_wish` / `checkin` / `unlock_badge` / `growth_review` 事件，喂给既有 `getStats` 看板。
4. **评审**：本方案需经用户确认范围与分期，再进入 Plan 详细设计 + 代码实现。

---

*附：本分析基于《Star_Rewards_愿望清单体系.pdf》V2 全人版；当前系统事实取自 `database/mysql.sql`、`script.js`（addGift/redeemGift/buildGrowthEvents/computeBestStreak）及项目工作记忆。*
