# Star-Rewards 网站功能全面测试方案

> 日期：2026-08-11 ｜ 测试对象：生产 `https://stellar.gaocaihk.com/`（当前版本 sw v34 / script?v27）
> 测试方式：真实浏览器（系统 Chrome 无头）驱动 + 截图比对 + API 冒烟，双端（桌面 1280×800 / 移动 375×667）

## 一、测试范围与优先级

| 优先级 | 模块 | 关注点 |
|---|---|---|
| P0 | 分享海报（已知 bug） | 成就徽章乱码（SVG 源码被当文本绘制）、头像/中文渲染、邀请码是否显示、统计卡布局 |
| P0 | 登录/注册 | 表单、邀请码预填、错误提示、去 emoji 化的 UI 效果 |
| P0 | 首页（登录态） | 顶栏、模块卡图标、积分趋势、成就区、家庭入口、solo 家庭「加入家庭」按钮 |
| P1 | 成长日历/日记 | 日历渲染、行为记录、删除回滚 |
| P1 | 愿望清单 | 卡片网格、无图占位图、进度条、兑换、兑换记录 |
| P1 | 成长纪念册 | 时间轴/月报/里程碑/童言，SVG 图标 |
| P1 | 成就徽章 | 13 枚 SVG 徽章渲染、解锁态、锁态 |
| P1 | 家庭共享 | 成员列表、邀请码/链接复制、加入家庭弹窗 |
| P2 | 行为模板页 ×2 | 73 模板、emoji 芯片、双语切换 |
| P2 | 证书 canvas | 达成证书渲染 |
| P2 | 响应式 | 移动端各页布局不溢出、顶栏折叠正常 |

## 二、测试手段

1. **代码审计**：优先静态定位高风险点（canvas 绘制 SVG 字符串、emoji 直绘、硬编码颜色、SQL/占位符）。
2. **浏览器实拍**：puppeteer-core 驱动系统 Chrome 无头，登录测试账号后逐模块截图；桌面 + 移动双视口。
3. **视觉比对**：人工检查每张截图（布局、图标、字体、溢出、对比度）。
4. **API 冒烟**：对改动点做生产接口回归（沿用此前冒烟脚本）。
5. **问题闭环**：发现 → 修复 → 重新截图验证 → 部署 → 线上复核。

## 三、测试账号
- 登录：`sr.test.owner.<ts>@example.com` / `TestPass123!`（此前完备性测试创建，含行为/愿望/兑换/成长记录数据，便于看到完整 UI 状态）。

## 四、交付物
- 每页截图（存 `screenshots/` 供比对）
- 本方案 → 测试执行 → 缺陷清单 → 修复记录汇总为最终报告
